PORT=5000
DB_HOST=localhost
DB_USER=carrera_app
DB_PASSWORD=Carrera@App2024
DB_NAME=career_assessment_db
JWT_SECRET=super_secret_key_for_authentication
NODE_ENV=development

# Admin Company ID — This is the secret code a person must enter
# to register as an admin. Never put this in frontend code.
ADMIN_COMPANY_ID=ritk7856$^

# Scoring version — bump this when questions or weights change
# so old results stay linked to the version they were calculated under
SCORING_VERSION=v1.0

# File upload directory for profile pictures
UPLOAD_DIR=./uploads